{
  "name": "ledger-api",
  "version": "1.0.0",
  "description": "Calorie-tracking API for the Ledger app — proxies food-data providers behind one normalized interface.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "multer": "^2.0.0"
  }
}
